kind: BUG FIXES
body: '`Workspace`: Fix an issue where the controller created a new destroy run on every reconcile when the status update after creating the destroy run failed, instead of reusing the destroy run already queued on the workspace. The queued destroy run is now found by listing the workspace runs, so it is also reused when it waits behind another run holding the workspace queue and is therefore not the workspace''s current run.'
time: 2026-09-03T15:32:43.000000000Z
custom:
    PR: "6"
